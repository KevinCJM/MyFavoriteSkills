# Project profile — optional controller cache

This is a manually maintained convention, not automatically loaded MCP configuration.
Reuse only when its repository and relevant revision/configuration remain valid.

- Repository identity / canonical server root:
- Local-to-server path mapping / shared artifact paths:
- Applicable root and nested AGENTS.md:
- Architecture / shared interface entry points:
- Worker model display label: DeepSeek V4.1 Flash (operator intention, not API ID)
- Worker variant/reasoning effort: <operator-selected value, or unset; do not invent `max`>
- Verified providerID / modelID / source / verification date:
- Model binding policy: verified_default | explicit_pair
- Effective model metadata observable: yes | no
- Verified Builder agent / permissions:
- Verified read-only agent / artifact-only permissions:
- Source upload/data-sharing constraints:
- Test/lint/type/build/benchmark commands and working directories:
- Relevant lockfiles/toolchain/fixtures:
- Four-question rule available to both Leader and Worker / authoritative source:
- Relevant API/config/log/data/dependency/workflow/compatibility contracts and evidence:
- High-risk domains and invariants:
- Worktree location preference / commit-integration permissions:
- Unique test resources or serialization requirements:
- Existing baseline failures and evidence:
- Last validated revision/configuration identity:

Never persist secret values. Reference approved credential mechanisms by name only.
Do not copy this profile into every prompt in full; pass only task-relevant entries.
