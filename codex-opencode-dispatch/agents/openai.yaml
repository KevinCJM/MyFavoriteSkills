interface:
  display_name: "Codex 派工与验收"
  short_description: "首次明确授权后，在同一对话和项目内按需委派并复用 OpenCode session"
  default_prompt: "使用 $codex-opencode-dispatch 处理当前对话与项目后续任务：每项改动先回答四问，按需主动派工，相关工作复用兼容 session，并由 Codex 验收实际证据。"
policy:
  allow_implicit_invocation: false
