# Tools Reference

<!-- Generated by scripts/generate-tool-docs.mjs. Run npm run docs:generate; do not edit directly. -->

The full profile exposes **87 tools**; the essential profile exposes **25 tools**. This reference is generated from the registered input/output schemas and behavior annotations.

Set `OPENCODE_TOOL_PROFILE=essential` for the smaller catalog; `full` is the default. Profile availability is listed for each tool. Profiles control discovery, not OpenCode permissions.

## Input and Output Contracts

When present, `directory` means an absolute path on the OpenCode server. Global authentication tools omit it; local project initialization uses `path`. See [configuration](configuration.md) for scope and [examples](examples.md) for workflows.

Results retain readable MCP `content` and provide `structuredContent`. Structured outputs include `text` and `isError`; generic JSON lives in `data`, while async tools provide identifiers and state fields. Tool errors also set the MCP result's `isError`. Inspect state rather than parsing text. An observation timeout can return a running state without an error or cancellation.

Async states are `accepted`, `running`, `input_required`, `completed`, `failed`, `cancelled`, and `unknown`. Native MCP task execution is negotiated separately from ordinary tool calls. See [architecture](architecture.md).

Annotations are behavior hints, not an authorization boundary. Nested structures and constraints are included in each tool's expandable JSON schema.

## Categories

- [Workflow Tools (13)](#workflow-tools)
- [Job Tools (4)](#job-tools)
- [Question Tools (3)](#question-tools)
- [Session Tools (20)](#session-tools)
- [Message Tools (6)](#message-tools)
- [File & Search Tools (6)](#file--search-tools)
- [Config Tools (3)](#config-tools)
- [Provider & Auth Tools (6)](#provider--auth-tools)
- [TUI Control Tools (9)](#tui-control-tools)
- [Project Tools (3)](#project-tools)
- [Event Tools (1)](#event-tools)
- [Global Tools (1)](#global-tools)
- [System & Monitoring Tools (12)](#system--monitoring-tools)

## Workflow Tools

### `opencode_ask`

Ask OpenCode a question in one step. Creates a new session, sends your prompt, and returns the AI response. This is the easiest way to interact with OpenCode.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `prompt` | string | yes | The question or instruction to send |
| `title` | string | no | Optional title for the session |
| `providerID` | string | no | Provider ID (e.g. 'anthropic') |
| `modelID` | string | no | Model ID (e.g. 'claude-3-5-sonnet-20241022') |
| `variant` | string | no | Model variant (e.g. 'fast', 'smart') |
| `agent` | string | no | Agent to use (e.g. 'build', 'plan') |
| `system` | string | no | Optional system prompt override |
| `format` | object \| object | no | Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool. |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "description": "The question or instruction to send"
      },
      "title": {
        "description": "Optional title for the session",
        "type": "string"
      },
      "providerID": {
        "description": "Provider ID (e.g. 'anthropic')",
        "type": "string"
      },
      "modelID": {
        "description": "Model ID (e.g. 'claude-3-5-sonnet-20241022')",
        "type": "string"
      },
      "variant": {
        "description": "Model variant (e.g. 'fast', 'smart')",
        "type": "string"
      },
      "agent": {
        "description": "Agent to use (e.g. 'build', 'plan')",
        "type": "string"
      },
      "system": {
        "description": "Optional system prompt override",
        "type": "string"
      },
      "format": {
        "description": "Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool.",
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "text"
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "json_schema"
              },
              "schema": {
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {}
              },
              "retryCount": {
                "type": "integer",
                "minimum": 0,
                "maximum": 9007199254740991
              }
            },
            "required": [
              "type",
              "schema"
            ],
            "additionalProperties": false
          }
        ]
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "prompt"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_check`

Get current task status, pending input, todos and file counts. Uses session summary metadata instead of fetching patches. Pass jobId for exact durable turn correlation; detailed includes response text.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | no | Session ID (required unless jobId is supplied) |
| `jobId` | string | no | Durable job ID |
| `messageId` | string | no | Submitted user message ID to correlate |
| `detailed` | boolean | no | Include response text (default false) |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `sessionId` | string | no |  |
| `jobId` | string | no |  |
| `messageId` | string | no |  |
| `directory` | string | no |  |
| `status` | "accepted" \| "running" \| "input_required" \| "completed" \| "failed" \| "cancelled" \| "unknown" | yes |  |
| `result` | unknown | no |  |
| `error` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "description": "Session ID (required unless jobId is supplied)",
        "type": "string"
      },
      "jobId": {
        "description": "Durable job ID",
        "type": "string"
      },
      "messageId": {
        "description": "Submitted user message ID to correlate",
        "type": "string"
      },
      "detailed": {
        "description": "Include response text (default false)",
        "type": "boolean"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "sessionId": {
        "type": "string"
      },
      "jobId": {
        "type": "string"
      },
      "messageId": {
        "type": "string"
      },
      "directory": {
        "type": "string"
      },
      "status": {
        "type": "string",
        "enum": [
          "accepted",
          "running",
          "input_required",
          "completed",
          "failed",
          "cancelled",
          "unknown"
        ]
      },
      "result": {},
      "error": {}
    },
    "required": [
      "text",
      "isError",
      "status"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_context`

Get full project context in one call: current project, path, VCS info, config, and available agents. Useful to understand the current state before starting work.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_conversation`

Get the full conversation history of a session, formatted for easy reading. Shows all messages with their roles and content.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | yes | Session ID |
| `limit` | number | no | Max messages to return (default: all) |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "Session ID"
      },
      "limit": {
        "description": "Max messages to return (default: all)",
        "type": "number"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "sessionId"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_fire`

Dispatch a durable background task and return its job, session and message IDs immediately. Use opencode_check or opencode_wait to observe; job_cancel stops the task.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `prompt` | string | yes | Task or instruction to send to OpenCode; minimum length: 1 |
| `sessionId` | string | no | Existing session to continue; omit to create one |
| `title` | string | no | Title for a new session |
| `providerID` | string | no | Provider ID from provider discovery |
| `modelID` | string | no | Model ID from provider discovery |
| `variant` | string | no | Model variant |
| `agent` | string | no | OpenCode agent name |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |
| `format` | object \| object | no | Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `sessionId` | string | no |  |
| `jobId` | string | no |  |
| `messageId` | string | no |  |
| `directory` | string | no |  |
| `status` | "accepted" \| "running" \| "input_required" \| "completed" \| "failed" \| "cancelled" \| "unknown" | yes |  |
| `result` | unknown | no |  |
| `error` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "minLength": 1,
        "description": "Task or instruction to send to OpenCode"
      },
      "sessionId": {
        "description": "Existing session to continue; omit to create one",
        "type": "string"
      },
      "title": {
        "description": "Title for a new session",
        "type": "string"
      },
      "providerID": {
        "description": "Provider ID from provider discovery",
        "type": "string"
      },
      "modelID": {
        "description": "Model ID from provider discovery",
        "type": "string"
      },
      "variant": {
        "description": "Model variant",
        "type": "string"
      },
      "agent": {
        "description": "OpenCode agent name",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      },
      "format": {
        "description": "Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool.",
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "text"
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "json_schema"
              },
              "schema": {
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {}
              },
              "retryCount": {
                "type": "integer",
                "minimum": 0,
                "maximum": 9007199254740991
              }
            },
            "required": [
              "type",
              "schema"
            ],
            "additionalProperties": false
          }
        ]
      }
    },
    "required": [
      "prompt"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "sessionId": {
        "type": "string"
      },
      "jobId": {
        "type": "string"
      },
      "messageId": {
        "type": "string"
      },
      "directory": {
        "type": "string"
      },
      "status": {
        "type": "string",
        "enum": [
          "accepted",
          "running",
          "input_required",
          "completed",
          "failed",
          "cancelled",
          "unknown"
        ]
      },
      "result": {},
      "error": {}
    },
    "required": [
      "text",
      "isError",
      "status"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_provider_test`

Quick-test whether a provider is working. Creates a temporary session, sends a trivial prompt, checks the response, and cleans up. Great for debugging auth issues.

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `providerId` | string | yes | Provider ID to test (e.g. 'anthropic', 'openrouter') |
| `modelID` | string | no | Specific model ID to test. If omitted, discovers the provider default or first available model. |
| `variant` | string | no | Model variant |
| `format` | object \| object | no | Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool. |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "providerId": {
        "type": "string",
        "description": "Provider ID to test (e.g. 'anthropic', 'openrouter')"
      },
      "modelID": {
        "description": "Specific model ID to test. If omitted, discovers the provider default or first available model.",
        "type": "string"
      },
      "variant": {
        "description": "Model variant",
        "type": "string"
      },
      "format": {
        "description": "Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool.",
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "text"
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "json_schema"
              },
              "schema": {
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {}
              },
              "retryCount": {
                "type": "integer",
                "minimum": 0,
                "maximum": 9007199254740991
              }
            },
            "required": [
              "type",
              "schema"
            ],
            "additionalProperties": false
          }
        ]
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "providerId"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_reply`

Send a follow-up message to an existing session. Use this to continue a conversation started with opencode_ask or opencode_session_create.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | yes | Session ID to reply in |
| `prompt` | string | yes | The follow-up message |
| `providerID` | string | no | Provider ID |
| `modelID` | string | no | Model ID |
| `variant` | string | no | Model variant |
| `agent` | string | no | Agent to use |
| `format` | object \| object | no | Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool. |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "Session ID to reply in"
      },
      "prompt": {
        "type": "string",
        "description": "The follow-up message"
      },
      "providerID": {
        "description": "Provider ID",
        "type": "string"
      },
      "modelID": {
        "description": "Model ID",
        "type": "string"
      },
      "variant": {
        "description": "Model variant",
        "type": "string"
      },
      "agent": {
        "description": "Agent to use",
        "type": "string"
      },
      "format": {
        "description": "Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool.",
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "text"
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "json_schema"
              },
              "schema": {
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {}
              },
              "retryCount": {
                "type": "integer",
                "minimum": 0,
                "maximum": 9007199254740991
              }
            },
            "required": [
              "type",
              "schema"
            ],
            "additionalProperties": false
          }
        ]
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "sessionId",
      "prompt"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_review_changes`

Get a formatted summary of all file changes made in a session. Shows diffs in a readable format.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | yes | Session ID |
| `messageID` | string | no | Specific message ID to get diff for |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "Session ID"
      },
      "messageID": {
        "description": "Specific message ID to get diff for",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "sessionId"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_run`

Send a task and wait for its correlated response. Returns a durable job ID; an observation timeout leaves the remote job running and can be resumed with opencode_wait.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `prompt` | string | yes | Task or instruction to send to OpenCode; minimum length: 1 |
| `sessionId` | string | no | Existing session to continue; omit to create one |
| `title` | string | no | Title for a new session |
| `providerID` | string | no | Provider ID from provider discovery |
| `modelID` | string | no | Model ID from provider discovery |
| `variant` | string | no | Model variant |
| `agent` | string | no | OpenCode agent name |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |
| `format` | object \| object | no | Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool. |
| `maxDurationSeconds` | number | no | Maximum observation duration in seconds (default 600); timeout does not abort the job; maximum: 3600 |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `sessionId` | string | no |  |
| `jobId` | string | no |  |
| `messageId` | string | no |  |
| `directory` | string | no |  |
| `status` | "accepted" \| "running" \| "input_required" \| "completed" \| "failed" \| "cancelled" \| "unknown" | yes |  |
| `result` | unknown | no |  |
| `error` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "prompt": {
        "type": "string",
        "minLength": 1,
        "description": "Task or instruction to send to OpenCode"
      },
      "sessionId": {
        "description": "Existing session to continue; omit to create one",
        "type": "string"
      },
      "title": {
        "description": "Title for a new session",
        "type": "string"
      },
      "providerID": {
        "description": "Provider ID from provider discovery",
        "type": "string"
      },
      "modelID": {
        "description": "Model ID from provider discovery",
        "type": "string"
      },
      "variant": {
        "description": "Model variant",
        "type": "string"
      },
      "agent": {
        "description": "OpenCode agent name",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      },
      "format": {
        "description": "Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool.",
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "text"
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "json_schema"
              },
              "schema": {
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {}
              },
              "retryCount": {
                "type": "integer",
                "minimum": 0,
                "maximum": 9007199254740991
              }
            },
            "required": [
              "type",
              "schema"
            ],
            "additionalProperties": false
          }
        ]
      },
      "maxDurationSeconds": {
        "description": "Maximum observation duration in seconds (default 600); timeout does not abort the job",
        "type": "number",
        "exclusiveMinimum": 0,
        "maximum": 3600
      }
    },
    "required": [
      "prompt"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "sessionId": {
        "type": "string"
      },
      "jobId": {
        "type": "string"
      },
      "messageId": {
        "type": "string"
      },
      "directory": {
        "type": "string"
      },
      "status": {
        "type": "string",
        "enum": [
          "accepted",
          "running",
          "input_required",
          "completed",
          "failed",
          "cancelled",
          "unknown"
        ]
      },
      "result": {},
      "error": {}
    },
    "required": [
      "text",
      "isError",
      "status"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_sessions_overview`

Get a quick overview of all sessions with their titles and status. Useful to find which session to continue working in.

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_setup`

Check OpenCode status, provider configuration, and optionally initialize a project directory. Use this as the first step when starting work — it tells you what is ready and what still needs configuration.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_status`

Get a quick status dashboard: server health, provider count, session count, and VCS info. Lighter than opencode_setup — good for at-a-glance checks.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_wait`

Wait for a session or durable job. Returns completed, failed, input_required, or a resumable timeout. Cancelling this wait stops observation; use job_cancel or session_abort to stop OpenCode.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | no | Session ID (required unless jobId is supplied) |
| `jobId` | string | no | Durable job ID returned by opencode_fire or opencode_run |
| `messageId` | string | no | Submitted user message ID to correlate the exact response |
| `timeoutSeconds` | number | no | Maximum seconds to wait (default 120); timeout is resumable; minimum: 0; maximum: 86400 |
| `pollIntervalMs` | integer | no | Polling interval in milliseconds (default 2000); minimum: 10; maximum: 60000 |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `sessionId` | string | no |  |
| `jobId` | string | no |  |
| `messageId` | string | no |  |
| `directory` | string | no |  |
| `status` | "accepted" \| "running" \| "input_required" \| "completed" \| "failed" \| "cancelled" \| "unknown" | yes |  |
| `result` | unknown | no |  |
| `error` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "description": "Session ID (required unless jobId is supplied)",
        "type": "string"
      },
      "jobId": {
        "description": "Durable job ID returned by opencode_fire or opencode_run",
        "type": "string"
      },
      "messageId": {
        "description": "Submitted user message ID to correlate the exact response",
        "type": "string"
      },
      "timeoutSeconds": {
        "description": "Maximum seconds to wait (default 120); timeout is resumable",
        "type": "number",
        "minimum": 0,
        "maximum": 86400
      },
      "pollIntervalMs": {
        "description": "Polling interval in milliseconds (default 2000)",
        "type": "integer",
        "minimum": 10,
        "maximum": 60000
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "sessionId": {
        "type": "string"
      },
      "jobId": {
        "type": "string"
      },
      "messageId": {
        "type": "string"
      },
      "directory": {
        "type": "string"
      },
      "status": {
        "type": "string",
        "enum": [
          "accepted",
          "running",
          "input_required",
          "completed",
          "failed",
          "cancelled",
          "unknown"
        ]
      },
      "result": {},
      "error": {}
    },
    "required": [
      "text",
      "isError",
      "status"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## Job Tools

### `opencode_job_cancel`

Explicitly abort the OpenCode session owned by a job. This also stops other work in that session.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `jobId` | string | yes | minimum length: 1 |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `sessionId` | string | no |  |
| `jobId` | string | no |  |
| `messageId` | string | no |  |
| `directory` | string | no |  |
| `status` | "accepted" \| "running" \| "input_required" \| "completed" \| "failed" \| "cancelled" \| "unknown" | yes |  |
| `result` | unknown | no |  |
| `error` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "jobId": {
        "type": "string",
        "minLength": 1
      }
    },
    "required": [
      "jobId"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "sessionId": {
        "type": "string"
      },
      "jobId": {
        "type": "string"
      },
      "messageId": {
        "type": "string"
      },
      "directory": {
        "type": "string"
      },
      "status": {
        "type": "string",
        "enum": [
          "accepted",
          "running",
          "input_required",
          "completed",
          "failed",
          "cancelled",
          "unknown"
        ]
      },
      "result": {},
      "error": {}
    },
    "required": [
      "text",
      "isError",
      "status"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_job_get`

Observe a durable job by ID, including pending inputs and final results.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `jobId` | string | yes | minimum length: 1 |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `sessionId` | string | no |  |
| `jobId` | string | no |  |
| `messageId` | string | no |  |
| `directory` | string | no |  |
| `status` | "accepted" \| "running" \| "input_required" \| "completed" \| "failed" \| "cancelled" \| "unknown" | yes |  |
| `result` | unknown | no |  |
| `error` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "jobId": {
        "type": "string",
        "minLength": 1
      }
    },
    "required": [
      "jobId"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "sessionId": {
        "type": "string"
      },
      "jobId": {
        "type": "string"
      },
      "messageId": {
        "type": "string"
      },
      "directory": {
        "type": "string"
      },
      "status": {
        "type": "string",
        "enum": [
          "accepted",
          "running",
          "input_required",
          "completed",
          "failed",
          "cancelled",
          "unknown"
        ]
      },
      "result": {},
      "error": {}
    },
    "required": [
      "text",
      "isError",
      "status"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_job_input`

Respond to a job's pending questions or permissions. Omit responses to request MCP forms when supported, or receive manual response instructions. Never approves automatically.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `jobId` | string | yes | minimum length: 1 |
| `responses` | array<object> | no |  |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "jobId": {
        "type": "string",
        "minLength": 1
      },
      "responses": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "minLength": 1
            },
            "kind": {
              "type": "string",
              "enum": [
                "question",
                "permission"
              ]
            },
            "answers": {
              "type": "array",
              "items": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            },
            "reply": {
              "type": "string",
              "enum": [
                "once",
                "always",
                "reject"
              ]
            },
            "reject": {
              "type": "boolean"
            }
          },
          "required": [
            "id",
            "kind"
          ],
          "additionalProperties": false
        }
      }
    },
    "required": [
      "jobId"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_job_list`

List locally retained jobs in this OpenCode server and credential scope.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `limit` | integer | no | maximum: 200 |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "limit": {
        "type": "integer",
        "exclusiveMinimum": 0,
        "maximum": 200
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## Question Tools

### `opencode_question_list`

List pending OpenCode questions, optionally filtered to a session.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | no |  |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_question_reject`

Reject a pending OpenCode question explicitly.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `requestId` | string | yes | minimum length: 1 |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "requestId": {
        "type": "string",
        "minLength": 1
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "requestId"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_question_reply`

Answer an OpenCode question request. Supply one array of selected labels or free text per question.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `requestId` | string | yes | minimum length: 1 |
| `answers` | array<array<string>> | yes |  |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "requestId": {
        "type": "string",
        "minLength": 1
      },
      "answers": {
        "type": "array",
        "items": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "requestId",
      "answers"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## Session Tools

### `opencode_permission_list`

List all pending permission requests across all sessions. When a session is blocked waiting for approval (e.g. to run a shell command or access a file outside the project), it appears here. Respond with `opencode_session_permission`.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_abort`

Abort a running session

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID to abort |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID to abort"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_children`

Get child sessions of a session

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Parent session ID |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Parent session ID"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_create`

Create a new session. Optionally provide a parentID to create a child session, and a title.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `parentID` | string | no | Parent session ID |
| `title` | string | no | Session title |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `sessionId` | string | yes |  |
| `session` | unknown | yes |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "parentID": {
        "description": "Parent session ID",
        "type": "string"
      },
      "title": {
        "description": "Session title",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "sessionId": {
        "type": "string"
      },
      "session": {}
    },
    "required": [
      "text",
      "isError",
      "sessionId",
      "session"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_delete`

Delete a session and all its data

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID to delete |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID to delete"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_diff`

Get the diff for a session, optionally for a specific message

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID |
| `messageID` | string | no | Message ID (optional) |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID"
      },
      "messageID": {
        "description": "Message ID (optional)",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_fork`

Fork an existing session, optionally at a specific message

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID to fork |
| `messageID` | string | no | Message ID to fork at (optional) |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID to fork"
      },
      "messageID": {
        "description": "Message ID to fork at (optional)",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_get`

Get details of a specific session by ID

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_init`

Analyze the app and create AGENTS.md for a session. NOTE: This is a long-running operation that may take 30-60+ seconds depending on project size.

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID |
| `messageID` | string | yes | Message ID |
| `providerID` | string | yes | Provider ID (e.g. 'anthropic') |
| `modelID` | string | yes | Model ID (e.g. 'claude-3-5-sonnet-20241022') |
| `variant` | string | no | Model variant (e.g. 'fast', 'smart') |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID"
      },
      "messageID": {
        "type": "string",
        "description": "Message ID"
      },
      "providerID": {
        "type": "string",
        "description": "Provider ID (e.g. 'anthropic')"
      },
      "modelID": {
        "type": "string",
        "description": "Model ID (e.g. 'claude-3-5-sonnet-20241022')"
      },
      "variant": {
        "description": "Model variant (e.g. 'fast', 'smart')",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id",
      "messageID",
      "providerID",
      "modelID"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_list`

List all sessions

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_permission`

Respond to a permission request in a session. Use `opencode_permission_list` to see pending requests. Reply values: 'once' (approve this request only), 'always' (approve this + future matching requests for this session), 'reject' (deny the request).

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID |
| `permissionID` | string | yes | Permission request ID |
| `reply` | "once" \| "always" \| "reject" | yes | Response to the permission request: 'once' to approve once, 'always' to auto-approve matching future requests, 'reject' to deny |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID"
      },
      "permissionID": {
        "type": "string",
        "description": "Permission request ID"
      },
      "reply": {
        "type": "string",
        "enum": [
          "once",
          "always",
          "reject"
        ],
        "description": "Response to the permission request: 'once' to approve once, 'always' to auto-approve matching future requests, 'reject' to deny"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id",
      "permissionID",
      "reply"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_revert`

Revert a message in a session

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID |
| `messageID` | string | yes | Message ID to revert |
| `partID` | string | no | Part ID to revert (optional) |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID"
      },
      "messageID": {
        "type": "string",
        "description": "Message ID to revert"
      },
      "partID": {
        "description": "Part ID to revert (optional)",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id",
      "messageID"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_search`

Search sessions by keyword in title. Useful for finding a specific session among many.

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `query` | string | yes | Search keyword (case-insensitive match on session title) |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search keyword (case-insensitive match on session title)"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "query"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_share`

Share a session publicly

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID to share |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID to share"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_status`

Get status for all sessions (running, idle, etc.)

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_summarize`

Summarize a session using a specified model. NOTE: This is a long-running operation that may take 30-60+ seconds.

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID |
| `providerID` | string | yes | Provider ID (e.g. 'anthropic') |
| `modelID` | string | yes | Model ID (e.g. 'claude-3-5-sonnet-20241022') |
| `variant` | string | no | Model variant (e.g. 'fast', 'smart') |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID"
      },
      "providerID": {
        "type": "string",
        "description": "Provider ID (e.g. 'anthropic')"
      },
      "modelID": {
        "type": "string",
        "description": "Model ID (e.g. 'claude-3-5-sonnet-20241022')"
      },
      "variant": {
        "description": "Model variant (e.g. 'fast', 'smart')",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id",
      "providerID",
      "modelID"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_todo`

Get the todo list for a session

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_unrevert`

Restore all reverted messages in a session

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_unshare`

Unshare a previously shared session

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID to unshare |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID to unshare"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_session_update`

Update session properties (e.g. title)

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Session ID |
| `title` | string | no | New title for the session |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Session ID"
      },
      "title": {
        "description": "New title for the session",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## Message Tools

### `opencode_command_execute`

Execute a slash command in a session (e.g. /init, /undo, /redo)

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | yes | Session ID |
| `command` | string | yes | The slash command to execute (e.g. 'init', 'undo') |
| `arguments` | string | no | Arguments for the command |
| `agent` | string | no | Agent to use |
| `providerID` | string | no | Provider ID |
| `modelID` | string | no | Model ID |
| `variant` | string | no | Model variant |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "Session ID"
      },
      "command": {
        "type": "string",
        "description": "The slash command to execute (e.g. 'init', 'undo')"
      },
      "arguments": {
        "description": "Arguments for the command",
        "type": "string"
      },
      "agent": {
        "description": "Agent to use",
        "type": "string"
      },
      "providerID": {
        "description": "Provider ID",
        "type": "string"
      },
      "modelID": {
        "description": "Model ID",
        "type": "string"
      },
      "variant": {
        "description": "Model variant",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "sessionId",
      "command"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_message_get`

Get details of a specific message in a session

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | yes | Session ID |
| `messageId` | string | yes | Message ID |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "Session ID"
      },
      "messageId": {
        "type": "string",
        "description": "Message ID"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "sessionId",
      "messageId"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_message_list`

List all messages in a session with formatted output showing roles and content

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | yes | Session ID |
| `limit` | number | no | Maximum number of messages to return |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "Session ID"
      },
      "limit": {
        "description": "Maximum number of messages to return",
        "type": "number"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "sessionId"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_message_send`

Send a prompt message to a session and wait for the AI response. Use parts to send text, and optionally specify a model.

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | yes | Session ID |
| `text` | string | yes | The text message to send |
| `providerID` | string | no | Provider ID (e.g. 'anthropic') |
| `modelID` | string | no | Model ID (e.g. 'claude-3-5-sonnet-20241022') |
| `variant` | string | no | Model variant (e.g. 'fast', 'smart') |
| `agent` | string | no | Agent to use |
| `noReply` | boolean | no | If true, inject context without triggering AI response (useful for plugins) |
| `system` | string | no | System prompt override |
| `format` | object \| object | no | Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool. |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "Session ID"
      },
      "text": {
        "type": "string",
        "description": "The text message to send"
      },
      "providerID": {
        "description": "Provider ID (e.g. 'anthropic')",
        "type": "string"
      },
      "modelID": {
        "description": "Model ID (e.g. 'claude-3-5-sonnet-20241022')",
        "type": "string"
      },
      "variant": {
        "description": "Model variant (e.g. 'fast', 'smart')",
        "type": "string"
      },
      "agent": {
        "description": "Agent to use",
        "type": "string"
      },
      "noReply": {
        "description": "If true, inject context without triggering AI response (useful for plugins)",
        "type": "boolean"
      },
      "system": {
        "description": "System prompt override",
        "type": "string"
      },
      "format": {
        "description": "Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool.",
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "text"
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "json_schema"
              },
              "schema": {
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {}
              },
              "retryCount": {
                "type": "integer",
                "minimum": 0,
                "maximum": 9007199254740991
              }
            },
            "required": [
              "type",
              "schema"
            ],
            "additionalProperties": false
          }
        ]
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "sessionId",
      "text"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_message_send_async`

Send a prompt asynchronously and return its messageId. Pass sessionId and messageId to opencode_wait to observe this exact turn.

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | yes | Session ID |
| `text` | string | yes | The text message to send |
| `providerID` | string | no | Provider ID (e.g. 'anthropic') |
| `modelID` | string | no | Model ID (e.g. 'claude-3-5-sonnet-20241022') |
| `variant` | string | no | Model variant (e.g. 'fast', 'smart') |
| `agent` | string | no | Agent to use |
| `format` | object \| object | no | Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool. |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "Session ID"
      },
      "text": {
        "type": "string",
        "description": "The text message to send"
      },
      "providerID": {
        "description": "Provider ID (e.g. 'anthropic')",
        "type": "string"
      },
      "modelID": {
        "description": "Model ID (e.g. 'claude-3-5-sonnet-20241022')",
        "type": "string"
      },
      "variant": {
        "description": "Model variant (e.g. 'fast', 'smart')",
        "type": "string"
      },
      "agent": {
        "description": "Agent to use",
        "type": "string"
      },
      "format": {
        "description": "Response format: plain text or JSON constrained by a JSON Schema. JSON Schema requires OpenCode permission for the StructuredOutput tool.",
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "text"
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": false
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "json_schema"
              },
              "schema": {
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {}
              },
              "retryCount": {
                "type": "integer",
                "minimum": 0,
                "maximum": 9007199254740991
              }
            },
            "required": [
              "type",
              "schema"
            ],
            "additionalProperties": false
          }
        ]
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "sessionId",
      "text"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_shell_execute`

Run a shell command through the opencode session

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `sessionId` | string | yes | Session ID |
| `command` | string | yes | Shell command to execute |
| `agent` | string | yes | Agent to use for the shell command |
| `providerID` | string | no | Provider ID |
| `modelID` | string | no | Model ID |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "Session ID"
      },
      "command": {
        "type": "string",
        "description": "Shell command to execute"
      },
      "agent": {
        "type": "string",
        "description": "Agent to use for the shell command"
      },
      "providerID": {
        "description": "Provider ID",
        "type": "string"
      },
      "modelID": {
        "description": "Model ID",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "sessionId",
      "command",
      "agent"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## File & Search Tools

### `opencode_file_list`

List files and directories at a path

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `path` | string | no | Path to list (defaults to project root) |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "path": {
        "description": "Path to list (defaults to project root)",
        "type": "string"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_file_read`

Read the content of a file

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `path` | string | yes | File path to read |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "File path to read"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "path"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_file_status`

Get status for tracked files (VCS changes: modified, added, deleted, etc.)

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_find_file`

Find files and directories by name (fuzzy match)

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `query` | string | yes | Search string for file/directory names |
| `type` | "file" \| "directory" | no | Limit results to 'file' or 'directory' |
| `searchDirectory` | string | no | Override the project root for the search |
| `limit` | number | no | Max number of results (1-200) |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search string for file/directory names"
      },
      "type": {
        "description": "Limit results to 'file' or 'directory'",
        "type": "string",
        "enum": [
          "file",
          "directory"
        ]
      },
      "searchDirectory": {
        "description": "Override the project root for the search",
        "type": "string"
      },
      "limit": {
        "description": "Max number of results (1-200)",
        "type": "number"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "query"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_find_symbol`

Find workspace symbols by name (functions, classes, variables, etc.)

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `query` | string | yes | Symbol name to search for |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Symbol name to search for"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "query"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_find_text`

Search for text patterns in project files (regex supported). Returns file paths, line numbers, and matching lines.

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `pattern` | string | yes | Text or regex pattern to search for in files |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "pattern": {
        "type": "string",
        "description": "Text or regex pattern to search for in files"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "pattern"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## Config Tools

### `opencode_config_get`

Get the current opencode configuration

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_config_providers`

List all configured providers and their default models

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_config_update`

Update the opencode configuration. Pass a partial config object with fields to update.

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `config` | object | yes | Partial config object with fields to update |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "config": {
        "type": "object",
        "propertyNames": {
          "type": "string"
        },
        "additionalProperties": {},
        "description": "Partial config object with fields to update"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "config"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## Provider & Auth Tools

### `opencode_auth_set`

Set authentication credentials for a provider (e.g. API key). Credentials are stored globally and shared across all projects.

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `providerId` | string | yes | Provider ID (e.g. 'anthropic') |
| `type` | string | yes | Auth type (e.g. 'api') |
| `key` | string | yes | API key or credential value |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "providerId": {
        "type": "string",
        "description": "Provider ID (e.g. 'anthropic')"
      },
      "type": {
        "type": "string",
        "description": "Auth type (e.g. 'api')"
      },
      "key": {
        "type": "string",
        "description": "API key or credential value"
      }
    },
    "required": [
      "providerId",
      "type",
      "key"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_provider_auth_methods`

Get available authentication methods for all providers

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_provider_list`

List all configured providers with their connection status. Returns a compact summary — use opencode_provider_models to see models for a specific provider.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_provider_models`

List available models for a specific provider. Call opencode_provider_list first to see provider IDs.

**Profiles:** full, essential.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `providerId` | string | yes | Provider ID (e.g. 'anthropic', 'openrouter', 'google') |
| `limit` | number | no | Max models to show (default 30). Use 0 for all. |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "providerId": {
        "type": "string",
        "description": "Provider ID (e.g. 'anthropic', 'openrouter', 'google')"
      },
      "limit": {
        "description": "Max models to show (default 30). Use 0 for all.",
        "type": "number"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "providerId"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_provider_oauth_authorize`

Start OAuth authorization for a provider

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `providerId` | string | yes | Provider ID to authorize |
| `method` | integer | yes | Auth method index from opencode_provider_auth_methods (default 0); minimum: 0; maximum: 9007199254740991; default: 0 |
| `inputs` | object | no | Additional inputs requested by the selected authentication method |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "providerId": {
        "type": "string",
        "description": "Provider ID to authorize"
      },
      "method": {
        "default": 0,
        "description": "Auth method index from opencode_provider_auth_methods (default 0)",
        "type": "integer",
        "minimum": 0,
        "maximum": 9007199254740991
      },
      "inputs": {
        "description": "Additional inputs requested by the selected authentication method",
        "type": "object",
        "propertyNames": {
          "type": "string"
        },
        "additionalProperties": {
          "type": "string"
        }
      }
    },
    "required": [
      "providerId",
      "method"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_provider_oauth_callback`

Handle OAuth callback for a provider

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `providerId` | string | yes | Provider ID |
| `callbackData` | object | yes | OAuth callback data |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "providerId": {
        "type": "string",
        "description": "Provider ID"
      },
      "callbackData": {
        "type": "object",
        "properties": {
          "method": {
            "type": "integer",
            "minimum": 0,
            "maximum": 9007199254740991,
            "description": "The method index used for authorization"
          },
          "code": {
            "description": "Authorization code for methods that request one",
            "type": "string"
          }
        },
        "required": [
          "method"
        ],
        "additionalProperties": false,
        "description": "OAuth callback data"
      }
    },
    "required": [
      "providerId",
      "callbackData"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## TUI Control Tools

### `opencode_tui_append_prompt`

Append text to the TUI's prompt input field

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes | Text to append to the prompt |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "Text to append to the prompt"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "text"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_tui_clear_prompt`

Clear the current prompt text in the TUI

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_tui_execute_command`

Execute a slash command through the TUI (e.g. '/init', '/undo')

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `command` | string | yes | Command to execute (e.g. '/init') |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "Command to execute (e.g. '/init')"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "command"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_tui_open_help`

Open the help dialog in the TUI

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_tui_open_models`

Open the model selector in the TUI

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_tui_open_sessions`

Open the session selector in the TUI

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_tui_open_themes`

Open the theme selector in the TUI

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_tui_show_toast`

Show a toast notification in the TUI

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `message` | string | yes | Toast message text |
| `title` | string | no | Optional toast title |
| `variant` | "info" \| "success" \| "warning" \| "error" | no | Toast variant (default: info) |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "Toast message text"
      },
      "title": {
        "description": "Optional toast title",
        "type": "string"
      },
      "variant": {
        "description": "Toast variant (default: info)",
        "type": "string",
        "enum": [
          "info",
          "success",
          "warning",
          "error"
        ]
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "message"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_tui_submit_prompt`

Submit the current prompt in the TUI (equivalent to pressing Enter)

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## Project Tools

### `opencode_project_current`

Get the current active project

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_project_init`

Initialize or open a project directory to host an independent OpenCode session. Use this to create new empty folders, or to explicitly open preexisting projects on the MCP host machine for parallel code generation workloads. This tool operates on the local filesystem; create remote server directories separately.

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `path` | string | yes | The absolute file path where the project directory is located or should be created. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "The absolute file path where the project directory is located or should be created."
      }
    },
    "required": [
      "path"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_project_list`

List all projects known to the opencode server

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## Event Tools

### `opencode_events_poll`

Poll project events from OpenCode, or explicitly select global scope. Collects up to maxEvents within the duration. Connection failures are reported with any partial events; stopping observation does not abort remote work.

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `durationMs` | integer | no | How long to collect events in milliseconds (default: 3000, max: 30000); minimum: 1; maximum: 30000 |
| `maxEvents` | integer | no | Maximum number of events to collect (default: 50, max: 1000); minimum: 1; maximum: 1000 |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |
| `scope` | "project" \| "global" | no | Event scope (default: project). Global events include all projects and cannot be combined with directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "durationMs": {
        "description": "How long to collect events in milliseconds (default: 3000, max: 30000)",
        "type": "integer",
        "minimum": 1,
        "maximum": 30000
      },
      "maxEvents": {
        "description": "Maximum number of events to collect (default: 50, max: 1000)",
        "type": "integer",
        "minimum": 1,
        "maximum": 1000
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      },
      "scope": {
        "description": "Event scope (default: project). Global events include all projects and cannot be combined with directory.",
        "type": "string",
        "enum": [
          "project",
          "global"
        ]
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## Global Tools

### `opencode_health`

Check server health and version

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

## System & Monitoring Tools

### `opencode_agent_list`

List all available agents with their names, descriptions, and modes (primary/subagent)

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_command_list`

List all available commands (built-in and custom slash commands)

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_formatter_status`

Get the status of configured formatters

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_instance_dispose`

Dispose the current opencode instance (shuts it down). WARNING: This is destructive and will terminate the server.

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_log`

Write a log entry to the opencode server

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `service` | string | yes | Service name for the log entry |
| `level` | "debug" \| "info" \| "warn" \| "error" | yes | Log level |
| `message` | string | yes | Log message |
| `extra` | object | no | Extra data to include in the log entry |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "service": {
        "type": "string",
        "description": "Service name for the log entry"
      },
      "level": {
        "type": "string",
        "enum": [
          "debug",
          "info",
          "warn",
          "error"
        ],
        "description": "Log level"
      },
      "message": {
        "type": "string",
        "description": "Log message"
      },
      "extra": {
        "description": "Extra data to include in the log entry",
        "type": "object",
        "propertyNames": {
          "type": "string"
        },
        "additionalProperties": {}
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "service",
      "level",
      "message"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_lsp_status`

Get the status of LSP (Language Server Protocol) servers

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_mcp_add`

Add an MCP server dynamically to opencode

**Profiles:** full.

**Annotations:** `readOnlyHint: false`, `destructiveHint: true`, `idempotentHint: false`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `name` | string | yes | Name for the MCP server |
| `config` | object | yes | MCP server configuration object |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name for the MCP server"
      },
      "config": {
        "type": "object",
        "propertyNames": {
          "type": "string"
        },
        "additionalProperties": {},
        "description": "MCP server configuration object"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "name",
      "config"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_mcp_status`

Get the status of all MCP servers configured in opencode

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_path_get`

Get the current working path of the opencode server

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_tool_ids`

List all available tool IDs that the LLM can use (experimental)

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_tool_list`

List tools with JSON schemas for a given provider and model (experimental)

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `provider` | string | yes | Provider ID |
| `model` | string | yes | Model ID |
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "provider": {
        "type": "string",
        "description": "Provider ID"
      },
      "model": {
        "type": "string",
        "description": "Model ID"
      },
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "required": [
      "provider",
      "model"
    ],
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>

### `opencode_vcs_info`

Get VCS (version control) info for the current project (branch, remote, status)

**Profiles:** full.

**Annotations:** `readOnlyHint: true`, `destructiveHint: false`, `idempotentHint: true`, `openWorldHint: true`.

#### Parameters

| Parameter | Type | Required | Description |
|---|---|---|---|
| `directory` | string | no | Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory. |

#### Structured output

| Field | Type | Required | Description |
|---|---|---|---|
| `text` | string | yes |  |
| `isError` | boolean | yes |  |
| `data` | unknown | no |  |

<details>
<summary>Complete input and output JSON schemas</summary>

```json
{
  "inputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "directory": {
        "description": "Absolute path to the project directory. When provided, the request targets that project. If omitted, the OpenCode server uses its own working directory.",
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "outputSchema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "properties": {
      "text": {
        "type": "string"
      },
      "isError": {
        "type": "boolean"
      },
      "data": {}
    },
    "required": [
      "text",
      "isError"
    ],
    "additionalProperties": {}
  }
}
```

</details>
