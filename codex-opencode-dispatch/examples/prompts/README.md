# 示例说明

本目录全部是**虚构的 fixture 项目**，路径、提交 ID、测试名和检查结果用于展示协议和离线测试，
不是用户项目的实际事实，也不表示任何这些测试已在 OpenCode 运行。

`builder.md` 是原任务，`rework.md` 和 `clarification.md` 与它关联。
其他文件分别展示紧凑、调查、设计和独立审核提示词。复制后必须用真实观测值替换示例内容。
纯格式检查通过不会验证示例路径/提交存在，也不会获得写权限或自动派工。
